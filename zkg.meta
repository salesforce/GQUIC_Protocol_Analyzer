[package]
description = Protocol analyzer that detects, dissects, fingerprints, and logs GQUIC traffic
tags = zeek plugin, protocol analyzer, gquic
plugin_dir = build/Salesforce_GQUIC.tgz
build_command = ./configure && make
depends =
  zkg >=2.0.7
  zeek >=3.1.0
